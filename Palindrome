#include<stdio.h>
int main()
{
    int n,c,sum=0,temp;
    scanf("%d",&n);
    temp=n;
    while(n>0){
        c=n%10;
        sum=(sum*10)+c;
        n=n/10;
    }
    if(temp==sum){
        printf("palindrome");
        
    }
    else{
        printf("not a palindrome");
    }
}
